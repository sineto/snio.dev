---
import { Icon } from "astro-icon/components";
import { type Card } from "@/data/about";
import { getLangFromUrl, useTranslations } from "@/i18n/utils";

interface Props {
  aboutStack: string[];
  aboutCards: Card[];
}

const { aboutStack, aboutCards } = Astro.props as Props;

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---
<section id="about">
  <div class="about-intro">
    <h2 class="section-header">{t("about.title")}</h2>
      <slot />
    <ul>
      {aboutStack && (aboutStack.map((item: string) => (
        <li>{item}</li>
      )))}
    </ul>
  </div>
  <div class="about-cards">
    {aboutCards && (aboutCards.map((card: Card) => (
      <ul>
        <li>
          <span>
            {card.title} <Icon name={card.iconName} />
          </span>
          <p>{card.tldr}</p>
        </li>
        <a href="">{t("about.card.clients")}</a>
      </ul>
    )))}
  </div>
</section>
